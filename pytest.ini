[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
testpaths = tests
pythonpath = db api engine queue
markers =
    integration: живой docker compose (SEEDING_RUN_COMPOSE_TESTS=1)
