---
description: Агент движка — libtorrent процесс, /internal/v1
globs: engine/**
alwaysApply: false
---

# Агент 5 — движок

- Внутренний HTTP под префиксом `/internal/v1/` (не для браузера). Публичный health можно дублировать для compose.
- Реальная раздача — в этой службе; API только шлёт команды и забирает статус.
- Готовность к SIGTERM: корректно закрывать сессию libtorrent (по мере внедрения).
